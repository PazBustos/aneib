<p>
  <strong>User:</strong>
  <%= @article.user.name %>
</p>

<p>
  <strong>Topic:</strong>
  <%= @article.topic.title %>
</p>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @article.description %>
</p>

<h3> Comentarios </h3>
	<%= render "comments/form" %>
	<ul>
		<% @article.comments.order("created_at DESC").each do |comment| %>
		<li>
			<%= comment.body %> - <%= comment.user.name %> - <p> Publicado: <%= time_ago_in_words(comment.created_at)%> atrás </p>
		</li>
		<%end%>

	</ul>




<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
